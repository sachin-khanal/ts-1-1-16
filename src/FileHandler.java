import java.util.List;

public interface FileHandler {

    //temporary interface replace with team member B's real implementation later.

    List<String> getAvailableFiles();
    String getFileContentByIndex(int index);
}

